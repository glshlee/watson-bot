# 📋 Watson (GitHub LifeLog AI Agent) PRD (Product Requirement Document)

## 1. 제품 개요 (Product Overview)
* **제품명**: Watson (GitHub LifeLog AI Agent System)
* **목적**: 24시간 가동되는 개인 서버 환경에서 AI 에이전트를 상시 운영하여, 노트북/PC 연결 없이도 텔레그램 봇 및 웹 대시보드를 통해 언제 어디서나 삶의 기록(Life Log)을 마크다운(MD) 형식으로 작성, 수정, 관리하고 GitHub 레포지토리에 자동 커밋/푸시(Git Commit & Push)하는 자동화 시스템 구축.
* **핵심 타겟**: 
  * GitHub 마크다운 파일 기반으로 일기, 운동, 업무, 생각 등의 라이프 로그를 작성하는 유저.
  * 24시간 서버 환경을 통해 언제 어디서나 모바일/웹으로 AI 에이전트를 호출해 정제된 라이프 로그를 남기고자 하는 유저.

---

## 2. 핵심 비전 및 유저 경험 (User Experience Goals)
1. **Anytime, Anywhere Access (어디서나 접근성)**
   * 모바일에서는 텔레그램 봇으로 메시지/사진/음성 메모 송신 ➔ AI가 라이프로그 정제 후 GitHub 즉시 반영.
   * PC/모바일 브라우저에서는 웹 대시보드(Watson)로 월별/일별 라이프 로그 시각화 및 AI 대화형 편집.
2. **Seamless Git Automation (투명한 Git 자동화 & 1기록 1커밋 - ADR-004)**
   * 단순 잡담/질의는 커밋하지 않으며, 비서가 감지/제안하여 사용자가 승인했거나 직접 기록한 유의미한 라이프로그에 한해 즉시 `git commit` & `git push` 자동 집행.
   * 다중 세션(텔레그램, 웹) 간 데이터 정합성을 위해 확정된 기록 즉시 안전하게 원격 반영.
3. **Smart Lifelog Butler (비서형 상호작용 & 마크다운 구조화)**
   * 수동 명령어 없이도 대화 중 운동, 미팅, 생각 등 가치 있는 일과를 비서가 감지하여 자연스럽게 기록을 제안(Proactive Suggestion).
   * 무작위 메모도 AI가 템플릿(일기, 운동 기록, 감상평, 캘린더 등)에 맞춰 일관성 있게 구조화된 MD로 작성.

---

## 3. 주요 기능 명세 (Key Features)

### 3.1. 🤖 AI 에이전트 코어 (Agent Core Engine)
* **Smart Lifelog Butler & Intent Router (지능형 비서 및 의도 분리기)**:
  * 대화 입력 시 [단순 잡담/질의], [일과 감지 및 기록 제안], [제안 승인], [직접 명령]을 자동 판별.
  * 제안된 후보(`pending_log`)를 세션 맥락에 보관하고, 승인 시 마크다운 반영 및 Git 커밋 파이프라인 트리거.
* **LifeLog Generator**: 입력된 메시지/데이터를 날짜별(`YYYY-MM-DD.md` 또는 `year/month/day.md`) 양식에 맞춰 변환/업데이트.
* **Git Operations Worker**: 백그라운드에서 repository pull ➔ MD 파일 변경 ➔ commit (`docs(lifelog): [카테고리] 요약 - YYYY-MM-DD`) ➔ push 수행.
* **Session & Context Management (다중 세션 대화 맥락 관리)**: 
  * 세션 ID 기반 대화 히스토리 및 맥락(Context Memory), 임시 기록 후보(`pending_log`) 영속적 유지.
  * 동일 세션 내 이전 대화 내용 및 대상 MD 파일 상태를 AI가 인지하여 자연스러운 연속 대화 및 덧붙이기(Append/Edit) 수행.
  * 텔레그램 채널별/웹 대시보드 대화방별 멀티 세션 독립 관리.

### 3.2. 💬 텔레그램 봇 인터페이스 (Telegram Bot Interface)
* `/log [내용]`: 라이프 로그 바로 기록.
* `/summary`: 최근 기록 요약 및 이번 주/달 분석 리포트 제공.
* `/status`: Git 서버 상태 및 최근 커밋 확인.
* 사진/이미지 및 메모 수신 시 AI 처리 후 적절한 MD 경로에 매핑 저장.

### 3.3. 🌐 웹 대시보드 (Web Dashboard - Watson)
* **대시보드 메인**: 최근 활동 스트림, 최근 깃 커밋 내역, 이번 달 라이프 로그 달력(Calendar View).
* **MD 라이프 로그 뷰어 & 에디터**: 렌더링된 마크다운 보기 및 실시간 수동 수정 기능.
* **에이전트 대화창**: 웹상에서 AI 에이전트와 직접 상호작용하며 라이프 로그 추가/질의응답/재정리 요청.

### 3.4. 📁 GTD 저장소 격리 및 동적 오케스트레이션 (GTD Directory Orchestration - ADR-007)
* **저장소-엔진 관심사 분리**: 왓슨 봇 소스코드 저장소와 개인 GTD/라이프로그 데이터를 완전히 분리.
* **동적 디렉토리 지정**: 웹 대시보드 UI 및 REST API(`GET/POST /api/settings/gtd-path`)를 통해 작업 GTD 디렉토리를 동적으로 변경/연결.
* **외부 GTD 체계 적응 및 오케스트레이션**: 연결된 레포지토리의 GTD 구조(`gtd/inbox.md`, `logs/daily/` 등)를 자동 감지하고 수집함 및 일일 로그에 맞춰 지능적 캡처/기록 오케스트레이션 수행.
* **독립 Git 격리**: 연결된 GTD 디렉토리가 독립 Git 레포지토리일 경우 해당 레포로만 커밋/푸시하고, 일반 디렉토리일 경우 로컬 파일로 안전하게 관리.

### 3.5. 📋 GTD 지능형 브리핑 및 일정 요약 (Task Briefing - ADR-008)
* **할 일/일정 실시간 조회**: 사용자가 "오늘 해야 할 일 정리해줘", "투두 목록", "일정 알려줘" 요청 시 GTD 저장소(`logs/daily/`, `gtd/next_actions.md`, `gtd/inbox.md`)를 즉시 파싱하여 종합 브리핑 제공.
* **Linux 환경 AGY CLI 100% 가동**: 백그라운드 데몬 및 리눅스 환경에서도 AGY 인텔리전스를 완전히 가동하여 자연스러운 대화 맥락 유지.

### 3.6. 🔄 GTD 저장소 원격 동기화 및 자동 Pull (Repo Sync - ADR-009)
* **온디맨드 레포 최신화**: 사용자가 "gtd 레포 최신화하고 다시 알려줘", "레포 최신화", "/sync" 요청 시 원격 GitHub로부터 최신 커밋을 `git pull --rebase --autostash`로 안전하게 동기화한 후 최신 상태 브리핑 제공.
* **브리핑 시 자동 최신화**: 할 일/일정 브리핑 요청 시 원격 저장소와 자동 동기화하여 멀티 디바이스(옵시디언, 모바일 등) 작업 내역 실시간 반영.

### 3.7. 🧠 세션 맥락 참조 기록 및 프롬프트 경량화 (Context-Aware Logging - ADR-010)
* **대화 맥락 참조 기록**: "아까 말한 내용도 기록해줘", "방금 이야기 일기에 적어줘" 등 이전 대화 맥락을 가리키는 요청 시 세션 히스토리에서 사용자 발화를 역추적하여 대상 마크다운 파일에 즉시 영속화 및 Git 커밋.
* **멀티라인 명령 수용 및 AGY 경량화**: 줄바꿈 및 복합 설명문이 포함된 입력을 안정적으로 파싱하고, 히스토리 토큰 경량화 및 타임아웃 50초 확장으로 지속적인 세션 대화 무결성 유지.

### 3.8. 🚀 결정론적 Git 푸시 및 AGY Headless 무중단 실행 (Repo Push - ADR-011)
* **결정론적 원격 푸시 의도 분기**: "푸시해줘", "푸시도 해줘", "깃 푸시", "/push", "푸시가 안됐는데 다시 확인해줘" 등 명시적 푸시 요청 시 LLM 환각(거짓말)을 차단하고 백엔드 `GitService.push()`를 즉시 실행하여 실시간 상태 보고.
* **Autostash 안전 병합 및 동기화 격리**: 로컬 작업 트리에 수정/언스테이징 파일이 있더라도 `autostash`를 통해 안전하게 병합 후 푸시 집행.
* **AGY Headless 권한 자동 승인**: `--dangerously-skip-permissions` 플래그 및 프롬프트 가드레일을 적용하여 비대화형 CLI 권한 거부 오류와 엉뚱한 폴백 표출을 원천 방지.

### 3.9. 🛡️ 테스트 샌드박스 격리 및 대화형 지시어 문맥 역추적 (Test Sandbox & Directive Resolution - ADR-012)
* **테스트 샌드박스 완벽 격리**: cURL 스모크 테스트 실행 시 임시 Git 저장소 샌드박스를 구성하여 테스트 더미 데이터가 사용자의 실제 개인 GTD 저장소에 유입되는 것을 100% 원천 차단.
* **구어체 지시어 문맥 역추적**: "응 오늘 로그에 기록해줘", "오늘 일기에 적어줘", "응 기록해줘" 등 지시어만 존재하는 요청 시 지시어 문구를 본문으로 오인하지 않고, 세션 히스토리에서 직전 나눈 긴 사연/감정을 역추적하여 안전하게 기록.

### 3.10. ⏱️ 한국 표준시(KST) 타임존 로컬라이제이션 (Timezone Localization - ADR-013)
* **KST(Asia/Seoul, UTC+9) 전면 동기화**: 서버의 기본 시간대(UTC)와 무관하게 모든 일일 로그 파일 경로(`YYYY-MM-DD.md`), 일기 기록 타임스탬프(`[HH:MM]`), 커밋 메시지 일자, 텔레그램 동기화 시간 및 브리핑을 한국 표준시로 정규화.
* **자정 경계(Midnight Boundary) 오류 원천 방지**: 한국 시각 기준 자정(00:00)부터 오전 09:00 사이 기록 시 UTC의 전날 날짜로 잘못 분기되는 결함을 완전히 차단하고 당일 일일 로그에 정상 기록.
* **환경 변수 기반 유연성**: `.env`의 `TIMEZONE` 설정을 통해 향후 글로벌 시간대 지원 및 다중 사용자 시간대 확장성 확보.

### 3.11. 🧭 복합 의도 감지 및 GTD 태스크 정제 (Compound Intent & Task Formulation - ADR-014)
* **데일리 로그 + GTD 동시 라우팅**: "로그와 gtd에 기록해줘", "일기랑 할일에 적어줘" 등 듀얼 요청 수신 시, 서사적 일기는 데일리 저널(`logs/daily/YYYY-MM-DD.md`)에, 실행 가능한 할 일은 GTD 인박스(`gtd/inbox.md`)에 단일 트랜잭션으로 동시 기록.
* **지시어 접미사(Suffix Leakage) 원천 차단**: 문장 끝부분의 지시어 수식구("로그와 gtd에 기록해줘") 및 구두점을 완벽히 절삭하여 깔끔한 일기 본문 보존.
* **행동 지향적 태스크 고속 정제**: 비정형 일기 사연 전체를 인박스에 복사하지 않고, 여행지/맛집/업무/구매 등의 핵심 요소를 추출하여 실행 가능한 포맷(`- [ ] 서산 여행 계획 및 맛집 방문 (용현집 어죽, 또간집 게국지) 🚗🍲`)으로 합성 및 적절한 도메인 섹션(`## 🧘 개인 생활 & 건강`)에 자동 배치.

### 3.12. 🔐 웹 콘솔 보안 인증 및 Cloudflare Tunnel 외부 연동 (Web Auth & Tunnel - ADR-015)
* **HTTP Basic 보안 인증 (ID/PW 가드)**: 브라우저 접속 시 네이티브 로그인 팝업을 통해 인가된 사용자만 웹 콘솔(`/`, `/api/*`)에 접근하도록 보호하여, 개인 일기와 GTD 태스크의 외부 유출 및 임의 조작 원천 차단.
* **무개방 Cloudflare Tunnel & 자동 HTTPS**: 클라우드 인바운드 방화벽 포트를 외부에 열지 않고 아웃바운드 터널로 암호화 연결을 생성하며, 무료 SSL(HTTPS) 인증서 기반으로 어디서나 모바일/PC 브라우저에서 안전 접속 보장.
* **무중단 systemd 백그라운드 구동**: `watson-tunnel.service`를 통해 왓슨 서비스와 함께 24/7 상시 터널링 가동.

### 3.13. 📱 모바일 퍼스트 반응형 웹 인터페이스 & 터치 최적화 (Mobile-First Responsive Web UX - ADR-016)
* **오프캔버스 드로어 & 모바일 햄버거 메뉴**: 스마트폰/태블릿 화면 폭(<= 768px)에서 사이드바를 슬라이드 드로어로 자동 전환하여 채팅 화면을 100% 확보하고, 세션 선택 및 새 세션 시작 시 드로어 자동 닫힘 제공.
* **동적 뷰포트(100dvh) 및 가상 키보드 최적화**: 모바일 브라우저 주소창 및 가상 키보드 팝업 시 레이아웃 깨짐을 방지하고, 16px 폰트 설정으로 iOS 사파리 자동 확대(Auto-zoom) 차단.
* **터치 친화적 UI 컴포넌트**: 모바일 헤더 축약 타이틀, GTD 배지 말줄임, 최소 38px~44px 터치 타깃 빠른 액션 버튼 및 자동 리사이징 텍스트에어리어 적용.

### 3.14. 🗂️ 웹 콘솔 세션 관리 고도화 (Advanced Session Management UI - ADR-017)
* **실시간 세션 검색 및 채널별 필터링**: 세션 제목, 고유 ID, 최근 메시지 내용을 기준으로 실시간 클라이언트 사이드 키워드 검색을 지원하고, `전체` / `🌐 웹` / `📱 텔레그램` 탭을 통한 채널별 원클릭 필터링 제공.
* **세션 제목 변경 (인라인/모달 편집)**: 직관적인 모달 대화창을 통해 세션 이름을 원하는 명칭으로 자유롭게 수정하고 실시간 UI 및 SQLite DB에 영속화.
* **세션 삭제 및 안전 폴백**: 특정 세션 및 하위 대화 메시지를 원자적으로 삭제하며, 현재 열린 세션 삭제 시 인접 세션 또는 기본 세션으로 안전하게 자동 전환.
* **대화 내역 비우기 (Clear Messages)**: 세션 메타데이터와 ID는 유지한 채 대화 메시지만 깔끔하게 초기화하여 새 대화 시작 지원.
* **첫 메시지 기반 자동 세션 네이밍**: 신규 세션에서 사용자가 첫 프롬프트를 전송하면 핵심 문구를 추출하여 가독성 높은 제목으로 자동 업데이트.
* **풍부한 세션 카드 및 헤더 메타 연동**: 메시지 개수, 상대적 경과 시간("방금", "5분 전", "어제"), 채널 배지를 표시하고, 헤더 영역에 활성 세션 정보 및 대화 비우기 바로가기 버튼 제공.

### 3.15. 🚀 에이전트 허브 대시보드 포털 및 개발 에이전트 분리 (Agent Hub & Dev Agent - ADR-018)
* **에이전트 허브 대시보드 포털 (`/`)**: 등록된 모든 AI 에이전트(Watson 비서, DevBot 개발자, 확장 슬롯)의 실시간 상태, 가동 지표, 주요 역량을 한눈에 모니터링하고 원클릭으로 전용 콘솔에 입장할 수 있는 상위 런처 포털 제공.
* **개발 전담 에이전트 DevBot 콘솔 (`/dev`)**: 소스코드 분석, Git 버전 관리(Status/Diff/Log/Branch) 빠른 실행 칩, 터미널 도구 자동화 및 기술 질의응답을 지원하는 전용 엔지니어링 콘솔 탑재.
* **왓슨 비서 콘솔 분리 (`/watson`)**: 일상 메모, 감정 회고, GTD 할 일 수집함 및 일정 브리핑을 담당하는 비서 콘솔을 독립 경로로 연결하고, 상단 헤더에 `[🏠 에이전트 허브]` 이동 링크 배치.
* **에이전트별 세션 및 역할 격리 (`agent_type`)**: SQLite `sessions` 테이블에 `agent_type` 컬럼을 도입하여 개인 일기/GTD 맥락과 개발 소스코드/Git 로그가 서로 간섭하지 않도록 물리적 격리 보장.

### 3.16. 🛠️ DevBot 대화형 엔지니어링 툴체인 및 실시간 개발 실행 환경 (Interactive Dev Toolchain - ADR-019)
* **대화형 엔지니어링 툴체인 단축 명령**:
  * `/test [경로]`: 터미널 접근 없이 DevBot 콘솔에서 pytest 단위 테스트를 즉시 실행하고 간결한 테스트 성공/실패/통계 브리핑 제공.
  * `/lint`: Ruff 정적 분석기 및 Mypy 타입 검사기를 병렬 실행하여 코드 무결성을 뱃지 형태로 보고.
  * `/commit [메시지]`: 현재 Git 워크스페이스의 변경점 및 diff를 분석하여 Conventional Commits 기반 추천 메시지 3종을 제안하거나 지정된 메시지로 즉시 안전 커밋 집행.
  * `/help`: 사용 가능한 모든 개발 툴체인 명령어 도움말 안내.
* **AGY CLI 호출 정규화 및 고탄력 엔지니어링 추론**:
  * 비대화형 실행 표준 인자(`-p "<prompt>" --dangerously-skip-permissions`), 50초 타임아웃, PATH 자동 보정 적용.
  * 워크스페이스 Git 상태, 브랜치, 변경 파일 수, 최근 커밋뿐 아니라 `docs/roadmap.md`의 최근 마일스톤 현황을 프롬프트 컨텍스트에 실시간 주입하여 살아있는 시니어 엔지니어 페어 프로그래밍 대화 제공.
* **웹 콘솔 UI 퀵 툴 칩 (Quick Action Chips)**:
  * `/dev` 웹 콘솔 하단 입력창 상단에 `/test`, `/lint`, `/commit`, `/help` 버튼을 배치하여 모바일/데스크톱 원터치 도구 실행 지원.
* **안전한 프로세스 격리 및 입력 인젝션 방지**:
  * 디렉토리 트래버설(`..`) 및 쉘 메타문자(`;`, `&`, `|`, `>` 등) 차단을 통해 외부 명령 인자 위변조 원천 차단.

### 3.17. 🛡️ 결정론적 GTD 태스크 삭제, 구어체 원격 푸시/커밋 라우팅 및 저장소 투명성 (Deterministic Task Removal & Git Transparency - ADR-020)
* **결정론적 GTD 태스크 삭제 (`gtd_remove`)**: "tiara_ad는 제거해", "주간보고 제거" 등 사용자 요청 시 실제 `gtd/inbox.md` 및 `gtd/next_actions.md` 파일에서 일치하는 태스크 라인을 찾아 물리적으로 안전하게 삭제하고, 원자적 Git 커밋/푸시 집행.
* **명시적 Git 커밋 명령 (`repo_commit`)**: "커밋해", "커밋", "/commit" 요청 시 실제 작업 트리 변경 상태를 검사하여 변경점이 있을 때만 커밋/푸시하고, 변경점이 없으면 "작업 트리가 깨끗합니다"라고 정직하게 안내.
* **구어체 원격 푸시 (`repo_push`) 및 저장소 정보 투명 브리핑**: "푸시도해야지", "푸시해야지", "올려야지" 등 한국어 구어체 어미를 전면 포괄하며, "어디다 푸시한거야?" 질문 시 실제 연결된 GitHub Remote URL(`https://github.com/...`)과 브랜치명, 최신 커밋 해시를 투명하게 제공하여 LLM 허위 환각(가상 브랜치 날조 등) 원천 차단.

### 3.18. 🌐 웹 연결 복원력, Cloudflare HTTP/2 터널 안정화 및 화면 복귀 자동 동기화 (Web Connection Resilience - ADR-021)
* **Cloudflare HTTP/2 전송 및 Uvicorn Keep-Alive 튜닝**:
  * `cloudflared` 통신 프로토콜을 QUIC UDP에서 TCP TLS 기반 HTTP/2로 강제하여 클라우드 NAT 게이트웨이 및 모바일 통신망의 무입력 포트 만료/유휴 드롭 현상을 원천 방지.
  * Uvicorn Keep-Alive 시간을 75초로 증대하고 동시성 제한을 100으로 설정하여 역방향 프록시와 백엔드 간 연결 닫힘 경합 및 502/504 Bad Gateway 완전 해소.
* **초경량 헬스체크 및 하트비트 모니터링**:
  * 무부하 고속 응답(`< 1ms`)의 `GET /api/health`, `GET /healthz` 엔드포인트를 제공하고 25초 주기 브라우저 하트비트를 통해 실시간 연결 상태 점검.
* **지능형 재시도 및 세션 히스토리 자동 복원 (`fetchWithRetry`)**:
  * 일시적 네트워크 순단 시 지수 백오프로 최대 2회 자동 재시도하며, 모바일 화면 꺼짐/백그라운드 복귀(`visibilitychange`, `online`) 시 서버에 이미 저장된 최신 AI 응답을 즉각 동기화하여 메시지 유실 방지.
* **실시간 연결 상태 UI**:
  * 3단계 펄스 인디케이터(`online`, `warning`, `offline`) 및 최상단 연결 상태 배너(`#connection-banner`)와 원클릭 재시도 버튼 제공.

### 3.19. 📋 GTD 및 데일리 로그 파일 즉시 열람 숏컷 & DevBot 대기시간 최적화 (Inspection Shortcuts - ADR-022)
* **결정론적 파일 직접 열람 API**:
  * `AgentService`에 `read_daily_log()`, `read_gtd_files()`, `read_gtd_and_daily_log()`를 탑재하여 실제 파일 경로, 수정 시각, 줄 수, 미완료 태스크 개수 및 코드 블록 마크다운을 LLM 지연 없이 0.01초 내에 반환.
* **슬래시 커맨드 및 자연어 라우팅**:
  * `/today`, `/daily`, "오늘 로그 보여줘", "오늘 일기 읽어줘" -> `daily_log_inspect`
  * `/gtd`, `/inbox`, "gtd 파일 보여줘", "인박스 파일 읽어줘" -> `gtd_inspect`
  * `/gtd-today`, `/today-gtd`, "gtd랑 오늘 로그 보여줘" -> `gtd_and_log_inspect`
  * `/briefing` 슬래시 커맨드 매핑 추가.
* **DevBot 툴체인 및 성능 최적화**:
  * `/dev` 콘솔에서도 동일한 `/today`, `/gtd` 숏컷 지원 및 대화 히스토리 200자 트리밍 & 35초 타임아웃 적용.
* **웹 콘솔 원터치 칩 바**:
  * Watson 비서 콘솔(`.watson-quick-bar`) 및 DevBot 콘솔(`.dev-quick-bar`)에 즉시 실행 칩 제공.

### 3.20. 📱 모바일 뷰포트 레이아웃 안정화 & 컴포넌트 충돌 방지 (Mobile Layout Stabilization - ADR-023)
* **상단 헤더 충돌 격리 & 슬림화**:
  * 모바일(`<= 768px`)에서 부제목 텍스트(`p#chat-subtitle`)를 자동 숨김 처리하여 텍스트 줄바꿈 충돌 차단.
  * 우측 액션 배지(홈, 대화 비우기, DevBot 전환)를 `34x34px` 정사각형 터치 아이콘 버튼으로 컴팩트화하고 GTD 경로 배지 너비를 50px로 축약해 헤더 좌우 겹침을 100% 해소.
* **하단 입력창 슬림화 (140px ➔ 65px) & 100% AI 자동 분류 전환**:
  - 수동 범주 선택기(`.category-bar`)를 완전히 제거하고 왓슨 LLM의 맥락 인식 자동 분류에 전담 위임.
  - Auto Git Push 뱃지를 상단 헤더 프로필로 이동하고 빠른 칩 바(`.watson-quick-bar`)와 텍스트 입력창만 배치하여 하단 높이를 60~65px로 최소화.
* **Flexbox Child Overflow 원천 차단**:
  * `.message`, `.bubble`, `.code-block`에 `min-width: 0; word-break: break-word; overflow-wrap: anywhere;` 적용.
  * 아바타 `flex-shrink: 0;` 고정 및 코드 블록 내부 스크롤 격리로 긴 코드/표 출력 시 화면 흔들림 및 컴포넌트 밀림 현상 박멸.
* **포털 대시보드 2x2 반응형 메트릭 그리드**:
  * 에이전트 허브 포털의 메트릭 카드 4종을 2x2 반응형 그리드로 정돈하고 자산 캐시 방지 쿼리 `v=1.3.3` 반영.

### 3.21. 🌅 아침/저녁 맞춤형 GTD 브리핑(Morning & Evening Briefing) 프로토타입 (ADR-024)
* **시간대(KST) 및 의도 기반 자동 모드 판별 (`detect_briefing_mode`)**:
  * 오전 05:00 ~ 13:59: 오늘 하루를 힘차게 시작하는 **아침 브리핑(Morning Briefing)** 자동 제공.
  * 오후 14:00 이후 및 야간: 오늘 하루 성과를 돌아보고 내일을 준비하는 **저녁 일과 회고(Evening Briefing)** 자동 제공.
  * `/briefing morning`, `/briefing evening` 및 자연어 질의("오늘 아침 브리핑", "오늘 저녁 회고 정리")를 통한 명시적 모드 오버라이드 지원.
* **아침 브리핑 핵심 가치 (Morning Focus)**:
  * 🎯 **오늘의 집중 우선순위 Top 3**: Next Actions에서 가장 시급하고 중요한 과제 선별.
  * ⏰ **오전/오후 추천 실행 흐름**: 일정과 딥워크 순서 제안.
  * 📥 **수집함 정리 안내**: 방치된 Inbox 항목 정리 권유.
* **저녁 회고 핵심 가치 (Evening Reflection & Rollover)**:
  * ✅ **오늘 완료된 작업 하이라이트**: 일일 로그 및 태스크 완료 성과 요약.
  * ⏳ **미완료 과제 점검 & 이월(Rollover)**: 내일로 넘길 과제 정돈.
  * 🎯 **내일 아침 1순위 핵심 과제 (Priority 1)**: 내일 시작할 첫 작업 제안.
* **AI 지능형 생성 & 0.01초 결정론적 포맷터 듀얼 엔진**:
  * LLM과 룰 기반 포맷터를 결합하여 무지연 고신뢰 응답 보장.
* **웹 콘솔 원터치 칩 및 REST API 지원**:
  * `[🌅 아침 브리핑]`, `[🌇 저녁 회고]` 원터치 칩 바 및 `GET /api/briefing?mode=morning|evening` 엔드포인트 제공.

### 3.22. ⏰ 브리핑 스케줄 UI 시각화 및 당일 일정 타임라인 (Briefing Schedule Visualization & Daily Timeline - ADR-025)
* **정규 브리핑 시각 및 활성 윈도우 명시화**:
  * 🌅 **아침 브리핑**: 매일 오전 **08:30 KST** (활성 감지 윈도우: 05:00 ~ 13:59 KST).
  * 🌇 **저녁 일과 회고**: 매일 오후 **20:00 (8:00 PM) KST** (활성 감지 윈도우: 14:00 ~ 04:59 KST).
* **무클릭 퀵 바 시간 뱃지 & 인터랙티브 스케줄 모달**:
  * 퀵 바 칩에 시간대를 직접 표기(`[🌅 아침 (08:30)]`, `[🌇 저녁 (20:00)]`)하여 한눈에 정규 시각 인지.
  * 신설된 `[⏰ 스케줄]` 칩을 탭하면 인터랙티브 팝업 모달(`#schedule-modal`)이 열려 현재 활성 모드 배지(`[🌅 아침 모드 진행 중]` 등), 아침/저녁 규격 상세, 일일 로그에서 파싱한 시간대별 일정 타임라인 목록을 시각화.
* **REST API 및 대화형 스케줄 라우팅**:
  * `GET /api/briefing/schedule` 경량 JSON 엔드포인트 제공.
  * `/schedule`, `/briefing schedule`, "몇 시에 스케줄링 되어있어?", "스케줄 확인" 질의 시 LLM 지연 없이 0.01초 내 스케줄 표 및 당일 일정 브리핑 반환.

### 3.23. 📲 텔레그램 정기 브리핑 자동 푸시 스케줄러 (Telegram Proactive Briefing Push Scheduler - ADR-026)
* **24/7 상시 능동형 푸시 발송 (Proactive Push)**:
  * 질문을 기다리지 않고, 매일 오전 **08:30 KST**(아침 브리핑)와 오후 **20:00 KST**(저녁 일과 회고) 정각에 왓슨이 먼저 스마트폰 텔레그램으로 브리핑 메시지를 자동 푸시 발송.
  * 일자별(`YYYY-MM-DD`) 발송 완료 상태 추적으로 하루 1회 중복 없는 정확한 발송 보장.
* **원격 Git 동기화 및 텔레그램 세션 영속화**:
  * 발송 직전 `git pull`을 수행하여 원격 최신 데이터를 반영하고, 발송된 메시지는 `telegram:{chat_id}` DB 세션에 저장되어 브리핑 직후 끊김 없는 후속 대화 지원.
* **REST API 및 웹 콘솔 원클릭 테스트**:
  * `GET /api/briefing/scheduler/status` (스케줄러 상태 및 수신자 수 조회).
  * `POST /api/briefing/trigger-push` (텔레그램 즉시 발송/시험 전송).
  * 웹 콘솔 스케줄 모달(`#schedule-modal`)에 텔레그램 연동 상태 표시 및 `[🔔 텔레그램으로 지금 즉시 발송]` 버튼 제공.

### 3.24. 🎛️ 텔레그램 인터랙티브 인라인 키보드 및 원클릭 태스크 조작 (Interactive Inline Keyboards - ADR-027)
* **정기 브리핑 무타자(Zero-Typing) 인라인 키보드**:
  * 🌅 **아침 브리핑(08:30 KST)**: `[✅ 1순위 태스크 완료]`, `[🔄 GTD 동기화]`, `[📋 전체 할 일 보기]`, `[🚀 원격 푸시]`
  * 🌇 **저녁 일과 회고(20:00 KST)**: `[📝 오늘 일기 작성]`, `[🔄 GTD 동기화]`, `[🚀 오늘 기록 푸시]`, `[📋 내일 할 일 보기]`
* **결정론적 태스크 완료 엔진 (`complete_top_task`)**:
  * Next Actions ➔ 일일 로그 ➔ 수집함(Inbox) 순으로 대기 중인 첫 번째 미완료 항목(`- [ ]`)을 찾아 `- [x]`로 안전하게 전환하고 원자적 Git 커밋/푸시 집행.
  * `/done` 및 `/done [태스크명]` 슬래시 커맨드를 통한 유연한 수동 제어 지원.
* **0.1초 고속 콜백 토스트 및 세션 정합성**:
  * 인라인 버튼 클릭 즉시 `answer_callback_query`로 즉각적인 토스트 피드백을 전달하고, 결과 메시지를 `telegram:{chat_id}` DB 세션에 저장하여 일관된 대화 맥락 보존.

### 3.25 기록 여부 결정론적 검사 및 문두 지시어 라우팅 (ADR-028)
* **기록 여부 질의 결정론적 라우팅 (`log_status_inspect`)**:
  * "오늘 로그 파일에 기록했어?", "기록했어?", "일기 적었어?", "기록 확인" 등 기록 상태 질의를 LLM 대화로 보내지 않고, 실제 디스크 파일 시스템(`logs/daily/YYYY-MM-DD.md`)을 직접 검사하여 사실만을 투명하게 보고.
  * 파일이 있고 일과가 기록된 경우 본문 전문 출력, 파일이 없는 경우 정직하게 미작성 상태를 안내하고 직전 대화 스니펫을 찾아 "응" 한마디로 즉시 기록할 수 있도록 복원 유도.
* **문두(Prefix) 기록 지시어 및 구어체 포괄 (`front_record_pattern`)**:
  * "어제 gtd에 이 내용을 넣어달라구 [본문]", "오늘 일기에 이거 적어줘: [본문]" 등 문장 시작부에 지시어가 오거나 "넣어줘", "업데이트 해줘" 등 자연스러운 구어체 요청 시 완벽히 본문을 추출하여 마크다운 기록 및 Git 커밋 집행.
* **AI 언어모델 시뮬레이션 원천 차단 가드레일**:
  * 백엔드 파일 기록이 실행되지 않은 상태에서 LLM이 가상으로 "정리해 두었습니다/기록했습니다"라고 응답하는 환각을 프롬프트 레벨에서 엄격히 금지.

### 3.26. 📝 2단계 사전 검토 및 원터치 승인 워크플로우 (Two-Phase Draft Review & Confirmation - ADR-029)
* **마크다운 초안 카드 (Draft Preview Card) 제안**:
  * 운동, 아이디어, 업무 성과, 가족/식사/병원 등 삶의 일과를 이야기하면 즉시 마크다운 파일을 수정하지 않고 `log_suggest` 인텐트로 전환하여 타임스탬프(`- [HH:MM]`), 저장될 대상 파일 경로, 카테고리가 정제된 마크다운 초안 카드를 사용자에게 사전 제시.
  * 일상 속 실천 과제(병원 예약, 물품 구입 등) 감지 시 일일 로그 초안과 GTD 인박스 초안(`- [ ]`)을 동시에 미리보기 카드로 구성.
* **텔레그램 & 웹 대시보드 원터치 인라인 액션 버튼**:
  * **텔레그램**: 인라인 키보드 `[✅ 응, 기록해줘]`, `[❌ 아니야]`를 메시지 하단에 동적 부착하여 터치 한 번으로 물리적 디스크 기록 및 원자적 Git 커밋/푸시 집행.
  * **웹 콘솔 (`/watson`)**: 제안 카드 수신 시 퀵 액션 버튼(`[응, 기록해줘]`, `[아니야]`) 동적 렌더링.
* **파워유저 패스트트랙 (`/log`)**:
  * 사전 검토를 생략하고 즉시 기록/커밋을 원하는 경우 `/log [내용]` 커맨드로 0초 만에 직행 가능.

### 3.27. 🚌 출근길 맞춤형 브리핑 설정 인터페이스 및 실시간 프리뷰 (Commute Briefing Interface - ADR-030)
* **맞춤형 동선 설정 영속화 (`CommuteConfigService`)**:
  * 배우자/사용자의 거주 지역(동네명, 기상 격자 X/Y 좌표, 대기 측정소명), 출근길 버스(탑승 정류소명, 정류소 ID/ARS번호, 버스 노선 번호, 도시코드), 정기 발송 시각(`07:30` KST, 평일 전용 토글), 공공데이터포털 API 키를 `config/commute_config.json`에 안전하게 영속화.
  * API 키는 마스킹(`****`) 처리하여 반환하며, 기존 등록된 키를 덮어쓰지 않고 안전하게 보존.
* **웹 대시보드 인터랙티브 모달 (`#commute-modal`)**:
  * 웹 콘솔 상단 헤더 배지(`[🚌 출근: 07:30]`) 및 퀵바 칩(`[🚌 출근길 설정]`)을 통해 1클릭으로 설정 모달 호출.
  * 깔끔한 4개 섹션(우리 동네, 출근길 버스, 발송 스케줄, 공공데이터 API 키) 폼 인터페이스 제공.
  * **실시간 미리보기 (`POST /api/settings/commute/preview`)**: `[🔍 실시간 미리보기]` 버튼 클릭 시 입력된 설정을 바탕으로 날씨·미세먼지·버스 도착 정보가 조합된 모닝 브리핑 카드를 모달 내 실시간 렌더링.
* **대화형 명령어 연동 (`/commute`)**:
  * `/commute`: 현재 출근길 브리핑 설정 현황 요약 및 웹 설정 링크 안내.
  * `/commute test` / `/commute preview`: 현재 설정 기준 실시간 출근 브리핑 카드 즉시 생성 출력.
* **무중단 시뮬레이션(Mock) 폴백**:
  * 공공데이터 API 키 미등록 또는 외부 API 점검/장애 시에도 스마트 시뮬레이션 데이터를 제공하여 브리핑이 중단되지 않는 회복탄력성 보장.

### 3.28. ✅ 구어체 태스크 완료 인식 고도화 및 항의·메타 피드백 가드레일 (Task Completion & Meta Guardrail - ADR-031)
* **구어체 태스크 완료 발화의 지능형 인식 (`task_complete`)**:
  * "민방위 사이버교육은 완료했어", "사이버교육 다했어", "보고서 제출 끝났어" 등 일상 구어체 완료 발화를 정확히 감지.
  * `[태스크] 완료 gtd에 기록해/반영해` 지시 시 신규 할 일을 추가하지 않고 기존 GTD 태스크를 찾아 `- [x]`로 즉시 완료 처리하고 Git 커밋·푸시 집행.
* **사용자 항의 / 메타 피드백 가드레일 (`meta_protest_patterns`)**:
  * "아니 이미 인박스에 있다면서. 그래서 완료했다고 말한건데?", "왜 새 할일로 등록해?" 등 봇의 이전 동작에 대한 항의/정정 발화 감지 시 절대로 일기 초안(`log_suggest`)으로 오인 제안하지 않음.
  * 이전 대화 히스토리를 역추적하여 사용자가 원래 요청했던 태스크 완료/삭제 작업을 즉시 자동 복구하고 정중한 사과 응답 제공.
* **유연한 토큰 매칭 및 서술어 자동 정제 (`complete_matching_tasks`)**:
  * 키워드에서 "완료했어", "끝났어", 조사 등을 자동 strip하고 2글자 이상 세부 토큰으로 분해하여 띄어쓰기나 어순이 달라도 GTD 태스크를 100% 매칭.
### 3.29. 📂 GTD 스킬 명세 동기화 및 태스크 완료 시 데일리 로그 수술적 이관 (Surgical Task Transfer - ADR-032)
* **스킬 행동 강령(Rules) 완벽 준수 및 단일 진실 공급원(SSOT) 분리**:
  * 미완료 할 일은 오직 `gtd/` 디렉토리 내 5개 상태 파일(`inbox.md`, `next_actions.md` 등)에서만 보관(SSOT).
  * 데일리 로그(`logs/daily/YYYY-MM-DD.md`)는 미완료 할 일을 남기거나 이월(Rollover)하지 않으며, 실제 완료된 성과만 모음.
* **결정론적 수술적 이동 (Surgical Transfer)**:
  * 태스크 완료("완료했어", "/done", 1순위 완료 등) 시 `gtd/` 파일에서 해당 항목을 **잘라내어(Cut)** 물리적으로 완전히 제거.
  * 당일 데일리 로그(`logs/daily/YYYY-MM-DD.md`)의 `## ✅ 오늘 완료한 일 (Completed GTD Tasks)` 섹션으로 `- [x]` 형태로 **이동(Paste)** 집행 (`transfer_completed_task_to_daily_log`).
* **Antigravity 네이티브 스킬 연동 및 시스템 프롬프트 자동 주입**:
  * `LLMProvider`가 GTD 경로 내 `skills/gtd-assistant/SKILL.md`를 자동 감지하여 핵심 행동 강령을 시스템 프롬프트에 `[적용 스킬: gtd-assistant]`로 상시 주입.
  * `agy` CLI 호출 시 서브프로세스 작업 디렉토리(`cwd`)를 사용자의 GTD 저장소 경로(`GTD_PATH`)로 지정하여 CLI의 네이티브 스킬 자동 탐색 및 로딩 지원.
### 3.30. ☀️ 아침 브리핑 실시간 날씨·미세먼지 통합 및 자연어 기상 질의 연동 (Morning Briefing Weather Integration - ADR-033)
* **모닝 브리핑 상단 실시간 날씨 & 미세먼지 카드 기본 통합**:
  * `BriefingService`의 아침 브리핑(`mode == "morning"`, 매일 아침 08:30 KST 텔레그램 푸시 및 `/briefing morning`) 최상단 인사말 직후 `#### 📍 오늘의 날씨 & 미세먼지` 블록을 필수로 통합 렌더링.
  * 기온, 체감기온, 하늘상태, 강수확률 및 우산 소지 팁, 미세먼지(PM10)/초미세먼지(PM2.5) 수치 및 측정소 기준 현황, 출근 버스 도착 예정 시각을 종합 브리핑.
* **AI 브리핑 엔진 날씨 누락 방어 및 필터 정상화**:
  * LLM 프롬프트에 실시간 날씨 요약을 주입하여 AI가 날씨 안내와 함께 하루를 열도록 지침 제공.
  * 기존 부적절한 `"날씨"` 배제 필터를 제거하고, AI 응답 내 날씨 누락 시 상단 자동 보강(Patch) 가드레일 적용.
* **자연어 기상 질의 지원 (`/weather`, `/commute`)**:
  * "오늘 날씨 어때?", "날씨랑 미세먼지 알려줘", "미세먼지 수치 확인" 등 질문 시 즉시 실시간 기상 카드를 제공하고, 일상 인사("안녕하세요! 오늘 날씨 좋네요")는 자연스러운 대화(`chat_only`)로 분리 유지.

### 3.31. 📍 동네 설정 스마트 지오코딩 및 대화형 위치 변경 연동 (Smart Location Geocoding & Conversational Setup - ADR-034)
* **한국 주요 행정구역 스마트 지오코딩 엔진 (`GeoService`)**:
  * 서울 25개 자치구 전체 좌표 및 측정소 완비, 주요 동(금호동, 상암동, 판교동 등) ➔ 자치구/시 스마트 역매핑.
  * 자연어 문장에서 불필요한 조사("우리 동네는", "으로 설정해줘")를 절삭하고 정규화된 `location_name`, `grid_x`, `grid_y`, `air_station_name`, `city_code`를 0.001초 내 결정론적 매핑.
* **대화형 동네 변경 인텐트 (`location_set`, `location_inspect`)**:
  * `/location [동네명]`, `/동네 [동네명]`, "우리 동네 성동구 금호동으로 설정해줘", "동네는 성동구 금호동인데 이렇게 그냥 설정하면 되는거야?" 등 일상 대화로 즉시 동네 변경 및 영속화.
  * 동네 변경 즉시 해당 지역 기준의 실시간 기상 브리핑 프리뷰 카드를 반환하여 사용자 안도감 제공.
  * `/location`, "우리 동네 어디로 되어있어?" 등 현재 거주지 조회 및 변경 안내 지원.
### 3.32. 🌤️ Open-Meteo 무설정 오픈 API 기반 실시간 날씨 및 대기질 연동 (Live Weather & Air Quality Integration - ADR-035)
* **Zero-Key 오픈 기상/대기질 엔진 (`WeatherService`)**:
  * API 키 발급 및 승인 대기 없는 Open-Meteo 글로벌 오픈 API를 연동하여 거주지 위경도 기반 실시간 기온, 체감온도, 하늘상태(WMO 코드 해석), 강수확률 및 실시간 미세먼지(PM10)/초미세먼지(PM2.5) 수치를 0.1초 내로 직접 조회.
* **한국 환경부 기준 대기질 등급 정규화 & 우산 팁 생성**:
  * 환경부 대기질 기준에 따라 좋음(<=30/15), 보통(<=80/35), 나쁨(<=150/75), 매우나쁨(>150/75) 등급 및 색상 이모지(🟢, 🟡, 🟠, 🔴)를 정규화하여 직관적 표시.
  * 일일 강수확률과 실시간 WMO 기상 코드를 결합하여 `우산 필수 ☔`, `접이식 우산 추천 🌂`, `우산 불필요 ☀️`를 결정론적으로 제안.
* **위경도 지오코딩 보강 & 10분 인메모리 캐싱**:
  * `GeoService` 내 서울 25개 자치구 및 전국 주요 지역의 위도(`lat`)와 경도(`lon`)를 내장하여 동네 매핑 시 자동 산출.
  * 10분(600초) TTL 인메모리 캐시 및 3.5초 타임아웃 기반 무중단 안전 폴백(Fallback) 구조 완비.
  * 브리핑 및 날씨 카드에 `(📡 Open-Meteo 실시간 라이브 API - HH:MM 기준)` 출처 배지를 명시하여 데이터 신뢰도 제공.

### 3.33. 🛡️ 키워드 정규식 가로채기 철거 및 LLM 자연어 위임 (Regex Interception Dismantling & LLM Delegation - ADR-036)
* **키워드 단순 매칭 가로채기(Interception Anti-Pattern) 철거**:
  * 기존 `workout_keywords`, `idea_keywords`, `work_keywords`, `life_keywords`로 단순 부분 일치(`in`) 기반 강제 `log_suggest`를 띄우던 정규식 누더기 블록을 전면 철거.
  * "구매할 필요가 없어"에서 "필[요가] 없어"의 "요가"가 매칭되어 엉뚱하게 운동 기록 초안이 생성되던 버그를 근본적으로 해결.
* **부정 / 불필요 / 취소 / 피드백 최우선 가드레일**:
  * `"필요 없어"`, `"필요가 없어"`, `"안 사도 돼"`, `"선물받아"`, `"취소"`, `"삭제"`, `"어때?"` 등 불필요/취소/피드백 발화 감지 시 일과 초안 생성을 원천 차단하고 LLM 대화로 직행.
* **서사적 완료 진술문(Narrative Action) 중심 초안 제안**:
  * 운동/삶의 기록 초안 제안은 실제 수행/완료했다는 서사 진술문(과거형/완료형 서술어 결합)에 한해 선별 제안하며, 그 외 모든 대화와 맥락은 LLM(Gemini / AGY)이 자연스럽게 판단하고 처리하도록 일원화.

### 3.34. 🚌 출근 버스 실시간 도착정보 API 실연동 및 지능형 캐시·안전 폴백 (Real-time Commute Bus Arrival API Integration - ADR-037)
* **서울시 TOPIS 및 국토교통부 TAGO 버스 API 실연동**:
  * 정류소 ID(ARS-ID)와 노선 번호 기반으로 실제 버스의 실시간 잔여시간(분/초), 남은 정류소 수, 막차/차고지 대기 상태를 실시간 조회 (`BusService`).
  * `urllib.parse.unquote()` 기반 이중 URL 인코딩 방지로 data.go.kr의 인증키 호환성 보장.
* **지능형 인메모리 캐싱 (45초 TTL)**:
  * 30~60초 주기의 버스 위치 특성을 반영하여 45초 TTL 인메모리 캐시를 적용, 0.01초 초고속 응답 및 API 쿼터 절약.
* **동적 스마트 시뮬레이션 및 안전 폴백**:
  * API 키 미등록 또는 외부 장애 시, 현재 분(minute)에 따라 가변적인 잔여 시간(3~11분)과 비서 출근 팁을 동적 계산하여 정적 4분 고정 버그를 탈피하고 안전한 폴백 제공.
* **단독 질의 지원 (`/bus`)**:
  * `/bus`, "출근 버스 언제 와?", "버스 도착 정보" 명령어로 출근 직전 단독 실시간 버스 도착 카드를 즉시 확인 가능.

### 3.35. 🚏 정류소 번호 원클릭 조회 및 지능형 정류소명 자동 매핑 (Smart Bus Stop Resolution & Sandbox Protection - ADR-038)
* **공공 검색 기반 지능형 정류소 역조회 엔진**:
  * 서울 5자리 ARS-ID 및 전국 정류소 번호 입력 시 공공 정류소 데이터베이스 및 지도 검색을 통해 `정류소명`, `방면`, `행정구역`을 0.1초 내 실시간 추출 (`BusService.resolve_bus_stop`).
  * 1시간 인메모리 캐시(`_stop_name_cache`)를 통해 중복 네트워크 요청 방지.
* **웹 콘솔 원클릭 조회 UI & 자동 보정**:
  * `POST /api/settings/commute/resolve-bus-stop` 엔드포인트 제공.
  * 모달 Section 2의 `[🔍 정류소 조회]` 버튼 및 4자리 이상 입력 시 blur/change 이벤트로 정류소명 입력란을 실시간 동적 자동 완성.
* **스모크 테스트 샌드박스 격리**:
  * `./scripts/smoke_test.sh` 실행 시 실제 사용자 `config/commute_config.json`을 임시 백업 및 테스트 종료 시 자동 복원하여 실제 등록된 API 키와 거주지/정류장 정보 영구 보존.

### 3.36. 🔄 실시간 버스 도착 정보 원터치 인라인 갱신 및 웹/텔레그램 동시 지원 (Realtime Bus Card Inline Refresh - ADR-039)
* **초 단위 실시간성 및 캐시 우회 (`force_refresh`)**:
  * 새로고침 버튼 터치 시 45초 인메모리 캐시를 즉각 우회하고 실시간 버스 API를 강제 호출(`force_refresh=True`).
  * 버스 카드 조회 일시에 초 단위 타임스탬프(`%H:%M:%S KST`)를 적용하여 갱신 시점을 직관적으로 확인 가능.
* **REST 기반 실시간 버스 카드 갱신 API (`/api/settings/commute/bus-card`)**:
  * `GET/POST /api/settings/commute/bus-card` 엔드포인트를 통해 최신 마크다운 카드, 요약 행(`transit_line`), 갱신 시각을 즉각 반환.
* **텔레그램 인플레이스 무중복 갱신 (`editMessageText`)**:
  * 버스 카드 및 아침 브리핑 하단에 `[🔄 실시간 버스 갱신]` 인라인 키보드 부착. 터치 시 신규 메시지 증식 없이 기존 메시지를 즉시 수정하고 0.1초 햅틱 토스트 피드백 제공.
* **웹 대시보드 콘솔 인라인 액션 바 & 퀵 칩**:
  * 버스 도착 정보가 포함된 어시스턴트 말풍선 하단에 `[🔄 버스 도착 갱신]` 버튼을 자동 부착하여 말풍선 본문을 인라인으로 즉시 업데이트하고 `[✅ 08:45:12 갱신됨]` 배지 시각화.
  * 왓슨 퀵 바에 `[🚍 버스 도착]` (`data-cmd="/bus"`) 원클릭 칩 신설.

### 3.37. 📱 텔레그램 네이티브 봇 메뉴 명령어(Bot Commands Menu) 등록 및 자동 동기화 (Telegram Bot Menu Commands - ADR-040)
* **표준 봇 명령어 14종 사전 정의 (`TelegramService.DEFAULT_COMMANDS`)**:
  * `today`, `briefing`, `bus`, `log`, `done`, `gtd`, `schedule`, `commute`, `sync`, `push`, `url`, `status`, `help`, `start` 등 핵심 14종 명령어와 한글 설명을 Telegram API 표준 포맷으로 완비.
* **Telegram Bot API 연동 및 자동 동기화 (`set_my_commands`)**:
  * `setMyCommands` 및 `setChatMenuButton(menu_button={"type": "commands"})` 호출을 통해 채팅창 좌측 하단 `[/]` 버튼 클릭 시 14종 명령어가 팝업 메뉴로 즉시 표시.
  * 봇 폴링 루프 시작 시(`start_polling`) 자동으로 최신 명령어 목록을 Telegram 서버에 등록/동기화.
* **REST API 엔드포인트 지원 (`/api/telegram/setup-commands`, `/api/telegram/commands`)**:
  * 봇 메뉴 명령어를 외부/UI에서 수동으로 등록(`POST`)하고 현재 등록 현황을 조회(`GET`)할 수 있는 관리 엔드포인트 제공.
* **GTD 메뉴 실행 지능화**:
  * 메뉴에서 `/gtd` 터치 시 단순 저장소 메타데이터가 아닌 실제 수집함(`inbox.md`) 및 다음 행동(`next_actions.md`) 목록을 즉시 브리핑.

### 3.38. 🎛️ 텔레그램 봇 메뉴 명령어 관리 UI 및 실시간 모바일 미리보기·원터치 동기화 (Telegram Commands Settings UI - ADR-041)
* **명령어 영속화 설정 파일 체계 (`config/telegram_commands.json`)**:
  * 사용자 정의 명령어 목록 및 각 명령어별 활성화 여부(`enabled`)를 JSON 파일로 안전하게 영속화하고 유효성 자동 검증(소문자 영문/숫자/언더스코어 1~32자).
* **웹 콘솔 전용 봇 메뉴 설정 모달 (`#telegram-menu-modal`)**:
  * 상단 헤더의 `[📱 메뉴: N개]` 배지 및 퀵 바 `[📱 텔레그램 메뉴]` 버튼을 통해 원터치로 모달 호출.
  * **좌측 명령어 편집기**: 체크박스 온/오프 토글, 명령어명 및 설명 인라인 편집, 삭제, `+ 새 명령어 추가` 지원.
  * **우측 모바일 실시간 미리보기**: 실제 텔레그램 모바일 프레임 내에 활성화된 명령어가 실시간 반응하여 변경사항 즉시 시각화.
  * **하단 액션**: `[기본값 복원]` 원클릭 리셋 및 `[텔레그램에 즉시 반영]` API 동기화 버튼 제공.
* **REST API 확장**:
  * `POST /api/telegram/commands` (저장 및 실시간 Telegram Bot API 동기화), `POST /api/telegram/commands/reset` (기본 14종 복원).

### 3.39. ⏳ GTD 마감일(Due Date / D-Day) 자동 감지 & 브리핑 알림 시스템 (GTD Due Date & D-Day Alert - ADR-042)
* **마감일 태그 포맷 파서 & 한국어 자연어 상대일자 계산 엔진 (`DueDateService`)**:
  * 마크다운 태스크 내 `~YYYY-MM-DD`, `~YYYY.MM.DD`, `@due(YYYY-MM-DD)` 명시적 태그 파싱.
  * "오늘까지", "내일까지", "모레까지", "이번 주 금요일까지", "다음 주 화요일까지", "N일 뒤까지" 등 한국어 상대일자를 KST 기준 정밀 날짜로 자동 변환.
* **4단계 긴급도 분류 및 지능형 우선순위 스코어링**:
  * `overdue` (D+N 기한 초과, 가중치 100), `today` (D-Day 오늘 마감, 가중치 80), `urgent` (D-1~D-3 마감 임박, 가중치 60-N), `upcoming` (D-4 이상) 4단계 자동 분류.
  * `next_actions.md` 및 브리핑 추천 시 마감 임박 과제를 최우선 집중 과제로 상단 자동 배치.
* **아침/저녁 브리핑 & GTD 실시간 요약 연동**:
  * 매일 08:30 아침 브리핑 상단에 `🚨 오늘 마감 D-Day`, `⚠️ 마감 임박 D-1~D-3`, `⛔ 기한 초과 Overdue` 경고 섹션 자동 주입.
  * 20:00 저녁 브리핑에서 미완료된 당일/초과 과제를 내일 최우선 이월 과제로 강조.
  * GTD 실시간 파일 읽기(`read_gtd_files`)에 `#### ⏳ 3. 마감일(D-Day) 현황` 섹션 추가.
* **단독 마감 점검 & 대화형/인라인 접근성**:
  * `/dday`, `/deadline`, "마감일 확인", "D-day 확인" 질의 시 전용 단독 리포트 반환.
  * 텔레그램 브리핑 하단 `[⏳ D-Day 마감 확인]` 원터치 인라인 버튼 및 웹 콘솔 왓슨/DevBot 퀵 칩(`[⏳ D-Day 마감]`) 제공.
  * 텔레그램 네이티브 봇 메뉴 15종 명령어로 `/dday` 기본 탑재.

### 3.40. 🔍 과거 라이프로그·GTD 고속 검색 및 주간 결산 회고 리포트 (Search & Weekly Review - ADR-043)
* **고속 전문 검색 엔진 (`SearchService`)**:
  * 일일 로그(`logs/daily/*.md`), GTD 문서(`gtd/*.md`, `inbox.md`), 프로젝트 폴더 대상 전체 텍스트 검색.
  * 공백 분리 다중 키워드 AND 매칭 및 대소문자 무시.
  * 본문 220자 문맥 스니펫 및 `**키워드**` 볼드 하이라이트 제공.
  * 완전 일치 가산점, 일일 로그 최신성(최근 30일), GTD 수집함 가중치 결합 관련도 정렬.
  * `/search [키워드]`, `/find [키워드]`, 자연어 질의("지난달 서산 맛집 찾아줘") 인텐트 자동 라우팅.
* **주간 결산 회고 리포트 엔진 (`WeeklyReviewService`)**:
  * 기준일로부터 지난 7일간의 기록 달성률(%), 완료한 GTD 태스크(`- [x]`) 전수 집계.
  * 🏃 운동, 💻 업무, 💡 생각/메모, 📝 일상 카테고리별 활동 정량 집계.
  * `gtd/inbox.md` 미분류 항목 체류 점검 및 `next_actions.md` 배치 권유.
  * LLM 및 룰 기반 하이브리드 "왓슨의 주간 한마디" 총평 합성.
* **매주 일요일 21:00 KST 능동 푸시 스케줄러 (`BriefingScheduler`)**:
  * 정기 스케줄러 루프를 통해 매주 일요일 21:00 KST에 주간 결산 리포트 능동 푸시 발송.
  * 인라인 액션 키보드(`[📥 Inbox 정리하기]`, `[⏳ D-Day 확인]`, `[🔄 원격 최신화]`, `[🚀 푸시]`) 제공.
* **REST API 및 UI 지원**:
  * `GET /api/search?q=키워드`, `GET /api/weekly?days=7`, `POST /api/weekly/trigger-push` 엔드포인트 제공.
  * 텔레그램 네이티브 봇 메뉴 17종(`search`, `weekly` 추가) 및 웹 콘솔 퀵 바 칩(`[🔍 기록 검색]`, `[📊 주간 결산]`) 지원.

### 3.41 사진·영수증·운동인증 Vision AI 멀티모달 분석 & 스마트 기록 (ADR-044)
* **Vision AI 5대 도메인 자동 분류 및 메트릭 파싱 (`VisionService`)**:
  * Gemini 1.5 Flash Vision REST API 멀티모달 프롬프트 기반 고속 시각 분석.
  * 🏃 **운동 인증 (`workout`)**: 러닝/헬스/인바디 화면에서 시간, 거리, 칼로리, 심박수 추출 및 `## 🏃 운동 & 건강` 섹션 표/불릿 포맷팅.
  * 🍲 **식사 & 맛집 (`meal`)**: 음식 메뉴, 식사 시간대 분석 및 미식 저널 포맷팅.
  * 🧾 **영수증 & 지출 (`receipt`)**: 상호명, 일시, 금액 추출 및 카드번호(`****-****-****-****`) 자동 마스킹, GTD 가계부 정리 태스크 자동 생성.
  * 📝 **메모 & 손글씨 (`memo`)**: 회의 화이트보드/포스트잇 OCR 및 GTD 검토 태스크 생성.
  * 🖼️ **일상 사진 (`general`)**: 일상 풍경 및 소중한 모먼트 요약.
* **2단계 사전 검토 워크플로우 연계 (ADR-029)**:
  * 사진 수신 시 즉시 저장 및 시각 분석 수행 후 초안 카드와 인라인 버튼(`[✅ 응, 기록해줘]`, `[❌ 아니야]`) 제시.
  * 캡션 내 `/log` 또는 `!` 포함 시 즉시 마크다운 반영 및 Git 커밋·푸시하는 패스트트랙 지원.
* **REST API 및 UI 연동**:
  * `POST /api/vision/analyze`: 사진 멀티파트 업로드 및 시각 분석 결과 JSON/초안 반환.
  * `POST /api/vision/upload-and-log`: 사진 업로드 후 사전 검토 보류 또는 패스트트랙 즉시 기록.
  * DevBot 전용 `/vision [경로] [캡션]` 툴체인 및 웹 콘솔 퀵 바 칩(`[📷 사진 분석]`) 추가.
### 3.42 웹 대시보드 연간 잔디(Heatmap) & 마크다운 인플레이스 에디터 (ADR-045)
* **연간 잔디 기여도 및 스트릭 집계 엔진 (`HeatmapService`)**:
  * 최근 365일간 저장소 내 모든 일일 로그 파일(`logs/daily/*.md`, `lifelogs/`) 전수 스캔.
  * 글자수 및 완료 태스크 기반 GitHub 표준 5단계(0~4) 기여도 레벨 정규화.
  * 현재 연속 기록 스트릭(`current_streak`), 역대 최장 스트릭(`longest_streak`), 총 기록 일수, 연간 달성률(%) 집계.
* **마크다운 인플레이스 파일 입출력 및 1기록 1커밋**:
  * 특정 일자 마크다운 원문 조회(`get_lifelog_content`) 및 미작성 일자 표준 기본 템플릿 자동 제공.
  * 인플레이스 디스크 저장 후 Git 커밋 및 선택적 원격 푸시 집행(`save_lifelog_content`).
* **웹 대시보드 반응형 인터랙티브 UI & 딥링크**:
  * 상단 헤더 잔디 뱃지 (`#heatmap-badge`) 및 퀵 바 원터치 칩 (`[✏️ 잔디 & 로그 편집]`).
  * 인터랙티브 모달 (`#lifelog-editor-modal`): 52주 잔디 그리드, 날짜별 호버 툴팁 및 셀 클릭 이동, 날짜 네비게이션, 좌우 분할 에디터(Textarea + 실시간 마크다운 렌더링), 원터치 커밋·푸시.
  * 딥링크 (`/?edit=YYYY-MM-DD`) 및 인텐트(`/edit`, `/heatmap`) 연동으로 즉각적인 일일 로그 수정 경로 제공.
* **REST API 및 명령어 20종 확장**:
  * `GET /api/lifelog/heatmap?days=365`, `GET /api/lifelog/file?date=YYYY-MM-DD`, `POST /api/lifelog/save`.
  * 텔레그램 네이티브 봇 메뉴 20종(`edit`, `heatmap` 추가) 및 DevBot 전용 `/edit`, `/heatmap` 툴체인 지원.

### 3.43 1-Click 셀프호스팅 배포 패키지 & 대화형 셋업 위저드 (ADR-046)
* **대화형 셋업 위저드 스크립트 (`./scripts/setup_wizard.sh`)**:
  * 터미널 환경에서 필수 도구 자동 확인 및 7단계 대화형 프롬프트(포트, 타임존, 봇 토큰, Chat ID, Gemini 키, GTD 경로, 동네명, 웹 보안)로 `.env` 및 `config/` 자동 생성.
  * `-y`/`--non-interactive` 무인 자동화 모드 및 `-d`/`--dry-run` 사전 검증 지원.
  * GTD 저장소 초기 디렉토리 구조(`logs/daily`, `gtd/inbox.md`, `gtd/next_actions.md`) 및 `git init` 자동 구성.
  * 호스트 환경(경로, 계정, 포트) 자동 적응 systemd 서비스 파일(`/tmp/watson.service`) 합성.
* **시스템 준비 상태 진단 엔진 (`SetupService`)**:
  * 텔레그램 봇, 관리자 ID, LLM 키, GTD 저장소 및 Git 연동, 웹 보안, 출근길 설정 등 8대 핵심 구성 요소 무결성 진단 및 준비율(`readiness_percentage`) 산출.
  * `GET /api/system/setup-status` REST API 및 DevBot `/setup` 명령어 툴체인 연동.
* **올인원 컨테이너 패키징 및 초보자 퀵스타트 가이드**:
  * `Dockerfile` (Python 3.12-slim, safe.directory, 헬스체크 내장) 및 `docker-compose.yml` 볼륨 영속화 완비.
  * 10분 만에 배포하는 초보자 가이드 (`docs/quickstart_guide.md`) 제공.

### 3.44. ⏰ 웹 콘솔 한국 표준시(KST) 동기화 및 실시간 시계 배지 연동 (Web Console Timezone & Time Synchronization - ADR-047)
* **세션 상대 시간 왜곡 원천 해결 ("방금" 정상화)**:
  * `SessionModel` 및 `ChatMessageModel` 생성/갱신 기본값을 `get_now()`(KST)로 통일하고, API 직렬화 시 KST 오프셋(`+09:00`)을 명시하여 브라우저에서 방금 보낸 메시지가 "9시간 전"으로 오표기되던 UTC 시간 왜곡 결함을 100% 해소.
  * 과거 생성된 세션 및 메시지 타임스탬프를 KST 기준으로 일괄 보정 마이그레이션.
* **상단 헤더 실시간 디지털 KST 시계 배지**:
  * 웹 콘솔(Watson `/watson` 및 DevBot `/dev`) 상단 헤더에 `#header-clock-badge`(`[⏰ 09:49:10 KST]`)를 탑재하여 초 단위 실시간 시간과 적용 타임존을 명확히 시각화.
* **일일 로그 에디터 날짜 판별 보정**:
  * 자정~오전 9시 사이 접속 시에도 `Intl.DateTimeFormat` 기반 KST 타임존 날짜(`YYYY-MM-DD`)를 정확히 계산하여 항상 당일 일일 로그가 열리도록 보장.
* **출근길 브리핑 스케줄러 능동 연동**:
  * `BriefingScheduler` 백그라운드 루프에서 `commute_config`의 발송 희망 시각(`send_time`, 예: `07:30 KST`) 일치를 감지하여 출근길 날씨·버스 브리핑을 텔레그램으로 능동 푸시 발송.
### 3.45. ⚡ 단위 테스트 초고속화 & 템플릿 컴포넌트 모듈화 리팩터링 (Test Speedup & Modular UI - ADR-048)
* **단위 테스트 격리 및 초고속화 (14분 27초 ➔ 2분 16초)**:
  * `tests/conftest.py`에 `fast_unit_test_environment` 글로벌 픽스처를 구축하여 단위 테스트 중 실제 `agy` CLI 바이너리 구동 및 원격 LLM 호출을 차단하고 스마트 결정론적 폴백 엔진으로 즉시 응답하도록 격리.
  * `DevAgentService`의 중첩 subprocess 실행(Mypy, Pytest) 및 `test_weather_service.py` 외부 네트워크 API 호출을 Mocking하여 전체 133개 테스트 소요 시간을 84% 단축(6.4배 가속).
* **UI 템플릿 8대 모달 컴포넌트 분할 (`app/templates/modals/`)**:
  * `index.html`에 집중되어 있던 400여 줄의 8개 모달 DOM(`rename`, `delete`, `clear`, `gtd`, `schedule`, `commute`, `telegram_menu`, `lifelog_editor`)을 독립 컴포넌트 파일로 분할하고 Jinja2 `{% include %}`로 조립하여 메인 템플릿 크기를 574줄에서 183줄로 68% 슬림화.

---


## 4. 시스템 아키텍처 개요 (System Architecture Overview)

```text
[ 모바일 텔레그램 앱 ]      [ 모바일 / PC 웹 브라우저 (GTD 경로 동적 설정) ]
          │                           │
          ▼                           ▼
  [ Telegram Bot API ]     [ FastAPI Web Dashboard ]
          │                           │
          └─────────────┬─────────────┘
                        ▼
            [ Watson Agent Core Engine ]
         (AI Intent / Multi-Session / GTD Orchestrator)
                        │
                        ▼
      [ Isolated GTD Storage / Dedicated Git Repo ]
       (gtd/inbox.md, logs/daily/YYYY-MM-DD.md etc.)
```

---

## 5. 성공 지표 (Key Success Metrics)
* **기록 소요 시간**: 모바일 텔레그램 메시지 보낸 후 GitHub 푸시 반영까지 5초 이내 완료.
* **시스템 가용성**: 24/7 서버 상시 가동 및 오류 발생 시 텔레그램으로 즉시 Fallback 알림.
* **사용자 만족도**: 텍스트 입력만으로 마크다운 구조화 및 깃 푸시 100% 성공.
